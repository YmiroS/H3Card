# 管理员初始化与启动说明

本文用于 Linux 中间层首次启用账号权限，或从无账号版本升级后的初始化。

适用环境：`start-controller.sh` 管理的 systemd 用户服务，服务直接运行 chouka 的 Python 后端。**这些命令在 Linux 中间层执行，不是在 Windows ComfyUI 节点执行。**

Web 画布和中间层控制面板由同一个服务提供，不需要再启动第二个 Web 进程。

> 当前 `start-controller.sh` 仅管理服务的启动、重启、状态和日志，尚未自动执行本文的依赖安装与账号初始化。首次启动请按下列顺序操作。

## 1. 确认服务路径和 Python 环境

以下以默认服务名 `h3card-controller.service` 为例。若实际服务名不同，请修改变量。

```bash
SERVICE_NAME="h3card-controller.service"

systemctl --user show "$SERVICE_NAME" \
  -p WorkingDirectory -p ExecStart -p EnvironmentFiles
```

确认：

- chouka 代码目录：通常是 `WorkingDirectory`，也可根据 `ExecStart` 中的 `server/app.py` 路径确定。
- Python 路径：必须使用 `ExecStart` 对应的解释器；如果服务使用虚拟环境，不能随意改用系统 Python。
- 环境配置文件：查看其中是否设置了 `CHOUKA_AUTH_DB`，但不要公开密码、令牌或完整密钥配置。

**先将下面的两个占位路径替换为实际路径，再执行：**

```bash
CHOUKA_DIR="/实际的/chouka/代码目录"
PYTHON="/服务实际使用的/python绝对路径"
ADMIN_USER="admin"

cd "$CHOUKA_DIR"
"$PYTHON" --version
```

默认账号库位于 `chouka/data/auth.db`：

```bash
AUTH_DB="$CHOUKA_DIR/data/auth.db"
```

如果服务配置了自定义 `CHOUKA_AUTH_DB`，请将 `AUTH_DB` 改成同一个数据库的绝对路径。本文每条账号命令都显式传入 `--database`，避免初始化到另一份账号库。

后续命令在同一个终端执行，以保留上述变量。

## 2. 停止旧服务并备份数据

初始化、迁移前先停止旧服务，避免迁移期间仍有任务或画布写入。若有生成任务，请先确认可以中断。

```bash
systemctl --user stop "$SERVICE_NAME"

BACKUP_DIR="$CHOUKA_DIR/data.backup.$(date +%Y%m%d-%H%M%S)"
if [ -d "$CHOUKA_DIR/data" ]; then
  cp -a -- "$CHOUKA_DIR/data" "$BACKUP_DIR"
  printf '数据已备份到：%s\n' "$BACKUP_DIR"
fi
```

如果账号库放在 `data` 目录之外，还需要在服务停止后单独备份该数据库，以及旁边可能存在的 `-wal`、`-shm` 文件。

**不要删除原有 `data`、`auth.db`、历史画布或计费数据库来跳过初始化问题。**

## 3. 安装依赖

```bash
"$PYTHON" -m pip install -r "$CHOUKA_DIR/requirements-controller.txt"
```

账号版本新增了 `argon2-cffi` 和 `dingtalk-stream` 等依赖。安装到其他 Python 或临时测试目录，不代表正式服务已经具备这些依赖。

如果提示系统 Python 受包管理器保护，请使用服务对应的虚拟环境，不要直接使用 `--break-system-packages` 强行安装。

## 4. 创建首个管理员

```bash
"$PYTHON" -m server.auth_admin init \
  --root "$CHOUKA_DIR" \
  --database "$AUTH_DB" \
  --username "$ADMIN_USER"
```

终端会要求输入两遍密码：

- 密码至少 12 个字符。
- 输入时不显示字符是正常现象。
- 没有默认密码；密码无需写进脚本、命令行参数、Git 或 SVN。

成功后会提示：

```text
管理员已创建；完成迁移前账号功能保持未就绪。
```

如果提示“管理员已初始化，不允许重复创建首管理员”，说明这份数据库已经存在管理员。**跳过 `init`，不要删除数据库重建。** 后续迁移的 `ADMIN_USER` 应使用已有的首个启用管理员账号。

## 5. 预检查历史权限迁移

创建管理员并不代表服务已经就绪，还需要给历史画布、任务和素材建立权限索引。

先执行预检查：

```bash
"$PYTHON" -m server.auth_admin migrate \
  --root "$CHOUKA_DIR" \
  --database "$AUTH_DB" \
  --owner "$ADMIN_USER" \
  --dry-run
```

`--dry-run` 不写入历史归属，也不会将账号系统设置为就绪。

确认报告中的以下四项都是空数组：

```json
{
  "missing": [],
  "ambiguous": [],
  "errors": [],
  "pending": []
}
```

| 字段 | 含义 |
| --- | --- |
| `missing` | 引用的上传文件或产物缺失 |
| `ambiguous` | 画布、任务归属或历史记录存在歧义 |
| `errors` | 历史数据无法解析或资源引用不合法 |
| `pending` | 仍有未完成可信登记的资源 |

如果其中任何一项非空，先处理报告中的具体问题，再重新预检查，**不要直接执行下一步或手工修改 `ready` 绕过检查**。

`projects`、`jobs`、`resources` 等正常列表不需要为空。`unknown_jobs` 表示无法归属某个画布的历史任务；迁移后这些任务仅管理员可见，不会伪造画布归属。

## 6. 正式迁移并确认就绪

仅在预检查通过后执行：

```bash
"$PYTHON" -m server.auth_admin migrate \
  --root "$CHOUKA_DIR" \
  --database "$AUTH_DB" \
  --owner "$ADMIN_USER" \
  --apply
```

迁移会为尚未登记的历史画布建立管理员归属，并登记任务和资源访问关系；不会把已有归属的画布统一改成新所有者。

然后检查账号库：

```bash
"$PYTHON" -m server.auth_admin audit \
  --root "$CHOUKA_DIR" \
  --database "$AUTH_DB"
```

必须确认最终输出包含：

```json
"ready": true
```

同时检查 `missing`、`pending` 为空。**不能只看 `audit` 的退出码，还要确认 `ready` 为 `true`。**

若迁移存在错误，系统会保持未就绪状态；修复问题后重新执行预检查和迁移。

## 7. 启动 Web 和中间层

```bash
SERVICE_NAME="$SERVICE_NAME" bash ./start-controller.sh start
```

默认访问地址：

- Web 画布：<http://dinghaichouka.youku.com:8199/>
- 中间层控制面板：<http://dinghaichouka.youku.com:8199/controller>

使用上面创建的管理员账号和密码登录。域名或端口不同时，换成实际部署地址。

脚本会显示服务状态并持续输出日志。按 `Ctrl+C` 只退出日志查看，**不会停止服务**。

本脚本启动的是 Linux 控制端，不会启动 Windows Worker。需要分布式生成时，应在控制端账号迁移就绪后再启动对应 Worker。

## 8. 日常使用与代码更新

初始化和迁移成功后，正常启动不需要重复创建管理员。

```bash
# 启动尚未运行的服务
SERVICE_NAME="$SERVICE_NAME" bash ./start-controller.sh start

# 更新后端代码后重启
SERVICE_NAME="$SERVICE_NAME" bash ./start-controller.sh restart

# 查看状态
SERVICE_NAME="$SERVICE_NAME" bash ./start-controller.sh status

# 查看日志
SERVICE_NAME="$SERVICE_NAME" bash ./start-controller.sh logs

# 停止服务
SERVICE_NAME="$SERVICE_NAME" bash ./start-controller.sh stop
```

**更新代码后不要只执行 `start`。** systemd 不会因此重启已经运行的 Python 进程，可能出现“前端已更新，后端仍是旧版本”的情况。更新依赖后执行 `restart`，再在浏览器按 `Ctrl+F5` 刷新。

## 9. 常见问题

### `/api/auth/me` 返回 404

通常是旧后端进程没有重启，或请求指向另一套服务/端口，不是管理员密码错误。

检查 `WorkingDirectory`、`ExecStart` 是否指向更新后的代码，然后重启服务。

### `/api/auth/me` 返回 401

未登录或会话已失效时属于正常响应。通过登录页面登录，不要直接把这个接口当作 Web 首页。

### 返回 503，提示初始化或迁移未完成

根据提示完成管理员初始化及权限迁移，并确认：

- 初始化命令与服务使用同一份账号库。
- `audit` 输出 `ready: true`。
- 不存在缺失文件、解析错误或未完成登记的资源。

### `ModuleNotFoundError: argon2` 或 `dingtalk_stream`

新增依赖没有安装到服务实际使用的 Python 环境。重新核对 `ExecStart`，使用同一个解释器安装 `requirements-controller.txt`。

### `No module named server.auth_admin` 或导入到 ComfyUI 的 `server.py`

确认当前目录是 chouka 仓库根目录，且使用合并后的完整代码（包含 `server/__init__.py`）。本文使用 `python -m server.auth_admin`，不要改成直接运行 `python server/auth_admin.py`。

### 提示“请求来源不可信”

使用域名或反向代理时，可在服务实际加载的环境配置中设置浏览器访问的完整来源，例如：

```text
CHOUKA_AUTH_ORIGIN=http://dinghaichouka.youku.com:8199
```

不要附加 `/api` 或其他路径。修改环境配置后重启服务；只修改 `.env.controller.example` 不会自动生效。

### 注册提示钉钉审批服务未配置

钉钉审批是可选功能，不是已有管理员登录的前置条件。未配置时，自助注册接口不可用；管理员仍可登录并管理账号。

需要启用自助注册时，参照 `.env.controller.example` 配置钉钉相关环境变量，再重启服务。

### 查看启动失败原因

```bash
systemctl --user --no-pager --full status "$SERVICE_NAME"
journalctl --user -u "$SERVICE_NAME" -n 80 --no-pager
```

排查时可提供错误日志，但不要公开密码、API 密钥、会话 Cookie、CSRF 令牌或完整环境配置。
