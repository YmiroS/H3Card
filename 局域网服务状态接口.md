# 局域网服务状态接口

用于让同一局域网内的其他软件查询抽卡服务当前是否空闲、是否有任务正在运行，以及有多少任务正在排队。

## 接口地址

```http
GET http://<服务器局域网IP>:8199/api/status
```

例如服务器的局域网 IP 是 `192.168.1.20`：

```text
http://192.168.1.20:8199/api/status
```

在服务所在的同一台电脑上可以使用：

```text
http://127.0.0.1:8199/api/status
```

抽卡系统启动时会在终端打印可用的局域网地址。服务监听 `0.0.0.0:8199`，因此局域网设备可以直接访问；如果连接失败，需要确认 Windows 防火墙允许 TCP 端口 `8199`。

## 请求

- 方法：`GET`
- 参数：无
- 身份验证：无
- 响应类型：`application/json`
- 缓存：禁用
- 浏览器跨域：允许，响应包含 `Access-Control-Allow-Origin: *`

## 响应示例

空闲状态：

```json
{
  "ok": true,
  "service": "chouka",
  "state": "idle",
  "idle": true,
  "busy": false,
  "running_count": 0,
  "queued_count": 0,
  "active_count": 0,
  "comfy_online": true
}
```

有 1 个任务正在运行、2 个任务正在排队：

```json
{
  "ok": true,
  "service": "chouka",
  "state": "busy",
  "idle": false,
  "busy": true,
  "running_count": 1,
  "queued_count": 2,
  "active_count": 3,
  "comfy_online": true
}
```

## 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| `ok` | boolean | 接口正常响应时固定为 `true` |
| `service` | string | 服务标识，固定为 `chouka` |
| `state` | string | `idle` 表示空闲，`busy` 表示已被任务占用 |
| `idle` | boolean | 没有运行和排队任务时为 `true` |
| `busy` | boolean | 存在运行或排队任务时为 `true` |
| `running_count` | integer | 当前正在运行的任务数量 |
| `queued_count` | integer | 当前正在排队的任务数量 |
| `active_count` | integer | 运行数量与排队数量之和 |
| `comfy_online` | boolean | 抽卡服务当前是否连接到 ComfyUI |

`idle` 和 `busy` 的判断规则：

```text
active_count = running_count + queued_count
idle = active_count == 0
busy = active_count > 0
```

如果接口请求失败或超时，应将状态显示为“服务离线/未知”，不能当成空闲。`comfy_online` 为 `false` 时表示抽卡服务仍可访问，但 ComfyUI 当前未连接，也不应提交生成任务。

## JavaScript 示例

```javascript
async function getServiceStatus() {
  const response = await fetch(
    "http://192.168.1.20:8199/api/status",
    { cache: "no-store" }
  );

  if (!response.ok) {
    throw new Error(`状态接口返回 ${response.status}`);
  }

  return response.json();
}

const status = await getServiceStatus();

if (!status.comfy_online) {
  console.log("ComfyUI 离线");
} else if (status.idle) {
  console.log("服务空闲");
} else {
  console.log(
    `服务占用中：运行 ${status.running_count} 个，排队 ${status.queued_count} 个`
  );
}
```

## Python 示例

```python
import requests

url = "http://192.168.1.20:8199/api/status"

try:
    response = requests.get(url, timeout=3)
    response.raise_for_status()
    status = response.json()
except (requests.RequestException, ValueError):
    print("服务离线或状态未知")
else:
    if not status["comfy_online"]:
        print("ComfyUI 离线")
    elif status["idle"]:
        print("服务空闲")
    else:
        print(
            f"服务占用中：运行 {status['running_count']} 个，"
            f"排队 {status['queued_count']} 个"
        )
```

## curl 测试

```bash
curl "http://192.168.1.20:8199/api/status"
```

## 建议查询频率

状态展示建议每 `2–5` 秒查询一次。接口只读取内存中的任务状态，不会创建任务、取消任务或修改项目。

## 统计范围

该接口统计通过抽卡系统提交并由抽卡系统管理的任务，包括图片、视频和文本处理任务。其他程序绕过抽卡系统、直接提交到 ComfyUI `8188` 端口的任务，不会进入这里的任务数量。