# 动态槽位设计方案

> **设计目标**：用户按需添加素材，界面不再一次性显示所有空槽位

## 一、数据结构

### Manifest 中新增的 `slotLimits` 字段

```json
{
  "id": "minimax_h3_ref_2pass",
  "name": "H3全能参考(高质量)",
  "slots": "img9+aud3+vid0",
  "slotLimits": {
    "image": {"min": 1, "max": 9},
    "audio": {"min": 0, "max": 3},
    "video": {"min": 0, "max": 0}
  },
  "inputs": [/* 只保留实际配置，前端动态生成界面 */]
}
```

### 字段说明

- `min`: 必填槽位数量（初始显示数量）
- `max`: 最大槽位数量（上限）
- `min=0`: 完全可选，初始不显示
- `min=1`: 至少需要1个，初始显示1个槽位

## 二、各模式的槽位配置

| 模式 | 图片 | 音频 | 视频 | 说明 |
|---|---|---|---|---|
| **文生图** | 0-0 | 0-0 | 0-0 | 无素材槽 |
| **图生图** | 1-1 | 0-0 | 0-0 | 固定1张图 |
| **参考图编辑** | 1-2 | 0-0 | 0-0 | 1张必填，可添加第2张 |
| **H3 图生视频** | 1-2 | 0-0 | 0-0 | 1张必填（阶梯：1张→图生视频，2张→首尾帧） |
| **H3 说话唱歌·单人** | 1-1 | 1-1 | 0-0 | 图片和音频各1个，都必填 |
| **H3 说话唱歌·双人** | 1-2 | 1-1 | 0-0 | 音频必填1个，图片1个必填可添加至2个 |
| **漫剧4宫格** | 1-5 | 0-0 | 0-0 | 1张必填，可添加至5张 |
| **四图拼四宫格** | 4-4 | 0-0 | 0-0 | 固定4张（拼图需求，不动态） |
| **H3全能参考(通用)** | 1-9 | 0-0 | 0-0 | 1张必填，可添加至9张 |
| **H3全能参考(高质量)** | 1-9 | 0-3 | 0-0 | 图片1张必填可添至9张，音频完全可选可添至3段 |

## 三、前端实现建议

### 3.1 初始渲染

```javascript
function renderSlots(capability) {
  const { slotLimits, inputs } = capability
  const slots = {
    images: [],
    audios: [],
    videos: []
  }
  
  // 初始只渲染 min 数量的槽位
  if (slotLimits.image.min > 0) {
    for (let i = 0; i < slotLimits.image.min; i++) {
      slots.images.push(renderSlot('image', i, inputs))
    }
  }
  
  if (slotLimits.audio.min > 0) {
    for (let i = 0; i < slotLimits.audio.min; i++) {
      slots.audios.push(renderSlot('audio', i, inputs))
    }
  }
  
  // 渲染"+ 添加素材"按钮
  renderAddButton(slotLimits, slots)
}
```

### 3.2 添加素材按钮

```javascript
function renderAddButton(slotLimits, currentSlots) {
  const options = []
  
  // 检查图片是否还能添加
  if (currentSlots.images.length < slotLimits.image.max) {
    const remaining = slotLimits.image.max - currentSlots.images.length
    options.push({
      type: 'image',
      icon: '📷',
      label: `添加图片（还可添加 ${remaining} 张）`
    })
  }
  
  // 检查音频是否还能添加
  if (currentSlots.audios.length < slotLimits.audio.max) {
    const remaining = slotLimits.audio.max - currentSlots.audios.length
    options.push({
      type: 'audio',
      icon: '🎵',
      label: `添加音频（还可添加 ${remaining} 段）`
    })
  }
  
  // 检查视频是否还能添加
  if (currentSlots.videos.length < slotLimits.video.max) {
    const remaining = slotLimits.video.max - currentSlots.videos.length
    options.push({
      type: 'video',
      icon: '🎬',
      label: `添加视频（还可添加 ${remaining} 个）`
    })
  }
  
  // 如果没有可添加的类型，隐藏按钮
  if (options.length === 0) {
    return null
  }
  
  return createAddButton(options)
}
```

### 3.3 点击添加

```javascript
function onAddSlot(type) {
  const currentCount = slots[type + 's'].length
  const limit = slotLimits[type]
  
  if (currentCount >= limit.max) {
    toast('已达到上限')
    return
  }
  
  // 创建新槽位
  const newSlot = renderSlot(type, currentCount, inputs)
  slots[type + 's'].push(newSlot)
  
  // 更新"添加"按钮状态
  updateAddButton()
}
```

### 3.4 删除槽位

```javascript
function onRemoveSlot(type, index) {
  const limit = slotLimits[type]
  
  // 必填槽位不能删除
  if (index < limit.min) {
    toast(`至少需要 ${limit.min} 个${typeLabel[type]}`)
    return
  }
  
  // 删除槽位
  slots[type + 's'].splice(index, 1)
  
  // 更新"添加"按钮状态
  updateAddButton()
}
```

### 3.5 提交 Payload

```javascript
function buildPayload(capability, slots, params) {
  const payload = {}
  
  // 只提交实际添加的槽位
  slots.images.forEach((slot, i) => {
    if (slot.file) {
      payload[`images[${i}]`] = slot.file
    }
  })
  
  slots.audios.forEach((slot, i) => {
    if (slot.file) {
      payload[`audio[${i}]`] = slot.file
    }
  })
  
  // 如果槽位为空且是可选的，需要在 API 调用时删除对应连接
  // 这个逻辑使用 manifest 中的 dropIfEmpty 配置
  
  return payload
}
```

## 四、特殊情况处理

### 4.1 阶梯模式（Ladder）

**H3 图生视频**：根据图片数量自动切换工作流
- 放1张图 → 自动切换到 `minimax_h3_i2v`（图生视频）
- 放2张图 → 自动切换到 `minimax_h3_flf2v`（首尾帧生视频）

前端需要：
1. 监听槽位数量变化
2. 根据 `ladder` 配置切换 `capability`
3. 重新加载对应的参数面板

### 4.2 固定槽位模式

**四图拼四宫格**：4张图全部必填，不支持动态添加
- `slotLimits.image: {min: 4, max: 4}`
- 前端直接显示4个槽位，不显示"添加"按钮

### 4.3 路由卡（Route Card）

**画质增强**：根据素材类型切换工作流
- 放图片 → 切换到图片放大工作流
- 放视频 → 切换到视频放大工作流

前端需要：
1. 初始显示一个通用"素材"槽位
2. 用户上传后检测类型
3. 切换到对应的 `capability`

## 五、优势总结

✅ **界面简洁**：初始只显示必填槽位，不堆砌空格子  
✅ **按需扩展**：用户需要几个就添加几个  
✅ **类型混合**：支持同时添加不同类型素材（图片+音频）  
✅ **上限控制**：达到上限后自动禁用对应选项  
✅ **符合直觉**：类似"添加附件"的常见交互模式  

## 六、后续工作

1. ✅ 扫描器已生成 `slotLimits` 元数据
2. ⏳ 前端实现动态槽位 UI
3. ⏳ 前端实现阶梯模式切换逻辑
4. ⏳ 前端实现路由卡切换逻辑
5. ⏳ 测试各模式的添加/删除/提交流程
