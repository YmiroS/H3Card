# 能力清单

> 这份文档是**给人看的索引**，不是数据源。真正的数据源是 `manifests/*.json`（由
> `server/scan_workflows.py` 扫 ComfyUI 工作流目录自动生成）。两边不一致时以 manifests 为准，
> 跑一次 `重新扫描工作流.bat` 重新生成即可。
>
> 想在一分钟内看到最新的真实情况，直接跑：
> ```bash
> curl -s http://127.0.0.1:8199/api/cards | python -m json.tool
> ```

## 画布上的操作

| 操作 | 怎么做 |
|---|---|
| 选中连线 | 点线本身。细线只有 1.3~1.6px，缩小后点不中，所以每根线底下垫一根 14 宽的透明粗线专管接鼠标（`path.hit`）。选中画成白色，右键出菜单（跳上游/跳下游/删线） |
| 删连线 | 选中后 **Delete**，或右键菜单。**删线不动下游那一格里的素材** —— 文件早就搬过去了，删线断的只是"上游重跑一次、下游自动换新产物"这条关系。素材也要清就右键那一格「清空这一格」 |
| 删卡片 | 选中后 **Delete**。跑出过东西的卡会先问一句（没有撤销，删掉那几轮记录就找不回来了）；空卡直接删。右键菜单里那条不问 —— 点菜单是有意的 |
| 复制粘贴 | **Ctrl+C** 拿在手上、**Ctrl+V** 粘在鼠标那儿（空白处右键第一条也是粘贴）。跨项目能粘，剪贴板只活在这一次打开里。**产物和历史不带过去**：新卡不该假装它也跑过。菜单里另有「就地复制一张」，那是立刻在旁边复制一张，两件事都留着 |

在输入框里打字时这些键一概不接（Ctrl+C 得留给复制文字，Delete 得留给删字）。

## 画布上有几张卡

卡片**不是**一个工作流一张。默认按「产出类型」归并：都出图的挤进「生图」卡里当模式，
都出视频的挤进「生视频」卡。只有玩法差别太大、不该藏在别人模式列表里第 N 项的，
才在 `scan_workflows.py` 的 `SOLO` 里点名单独成卡。

**工具卡自动识别，不用登记 `SOLO`。** `is_tool()` 的判据：这个能力**要素材、但不写提示词**。
生成类总得有句话描述要画什么；工具没有，改什么全看手上这份素材。「必须有素材」也不能少，
不然纯参数的工作流会被误判成工具。**种子不是判据** —— 补帧、重画放大内部都用扩散模型、
都要种子，但用户的诉求不是"创作一张新的"而是"把这份素材弄好点"，那就算工具。
命中的能力会拿到 `kind: "tool"`，于是：单独占一张卡、图标默认 🧩、
「新建卡片」菜单里单独进「工具」组（不混在生图里）。

| 卡片 | id | 图标 | 产出 | 模式 |
|---|---|---|---|---|
| 生图 | `card_image` | 🖼 | image | 文生图、图生图、多宫格故事分镜、参考图编辑(保人物) |
| 生视频 | `card_video` | 🎬 | video | H3 图生视频、H3 说话唱歌、漫剧4宫格 |
| H3 四图参考生视频 | `minimax_h3_ref4` | 🎬 | video | 就它自己 |
| H3全能参考(通用) | `minimax_h3_ref9` | 🎬 | video | 就它自己 |
| 四图拼四宫格 | `grid4_stitch` | 🧩 | image | 就它自己 |
| 视频补帧插针(GIMM-VFI) | `gimmvfi_interp` | 🧩 | video | 就它自己 |
| 画质增强 | `card_enhance` | ✨ | 跟着素材 | 图片/视频各走一条 SeedVR2 放大，见下面「路由卡」 |
| 风格化提示词 | `card_style` | 🎨 | 不出产物 | 不是能力，见下面「风格卡」 |

**每张卡的卡脚最左边常驻一枚「出图片 / 出视频」徽标**（`paintKind()`，图片=青、视频=洋红、
风格卡=紫「🎨 只加提示词」）。
卡片图标说明不了这件事 —— 工具卡一律 🧩、画质增强是 ✨；卡名还能被用户改成任意字。
徽标读的是**真正会跑的那条能力**（`CAPS[runCap(c)]`），不是卡片定义里的 `outputType` ——
路由卡的 `_cards.json` 里写死是第一路（image），照它画切到视频那一路后还报"图片"。
路由卡一格素材都没放时标「🖼🎬 跟素材」（`kindUndecided()`），放进去那一刻就落定。
没出过产物时画面区那个大图标（`phHTML()`）也是同一个信息，跟徽标一起换。

## 15 个能力

`slots` 记法：`img2+aud0+vid0` = 最多 2 个图槽、0 个音频槽、0 个视频槽。第一个槽必填；
其余槽能不能空由扫描器**顺着数据流往下走**来判（`slot_cuts()`）：碰到节点定义里 optional 的输入
就是一个「可断点」，碰到必填输入就继续往下问同一个问题，走到输出节点（`SaveImage` 那类）
就是断不了。**所有叶子都是可断点**这一槽才算能空。拼图节点的 `image1~4` 一路走到 `SaveImage`
都没有可断点，所以是必填 —— 空着跑必错，服务端直接回「必填素材未提供」，不会偷偷用模板里的演示图。

只看**直接消费者**那一跳是不够的：`flux2_klein_edit` 的第 2 张图要经过「缩放 → VAEEncode →
`ReferenceLatent.latent`」三跳，前两跳都是必填输入，真正能断的地方在第三跳。

「能空」和「能拔线」还是两件事。能空 = 不填也不报结构错；能拔（`dropIfEmpty`）= 把线整根拔掉、
这条支线没人依赖、ComfyUI 干脆不执行它，那才是"真的没参与"。`safe_cut()` 管后者：
拔掉的这根**跟节点自己的输出同类型就不能拔**（说明它就是被加工的那个东西）——
`ImageScaleByAspectRatio V2.image` 也声明成 optional，可它出 IMAGE，真拔了没东西可缩；
`ReferenceLatent` 少的是 LATENT、出的是 CONDITIONING，才是真的可有可无（少了就原样透传，
见 ComfyUI 的 `nodes_edit_model.py`）。一个槽可能要拔**好几根**（参考图编辑要拔正负两条
conditioning 上各挂的那一次），所以 `dropIfEmpty` 是个列表。

| 能力 id | 面板上的名字 | slots | 源工作流（`ComfyUI/user/default/workflows/` 下） |
|---|---|---|---|
| `zimage_t2i` | 文生图 | img0+aud0+vid0 | `4-Z-Image全系列/z-Image-标准版文生图.json` |
| `zimage_i2i` | 图生图 | img1+aud0+vid0 | `4-Z-Image全系列/Z-Image 图生图-反推 .json`（文件名末尾有个空格） |
| `flux2_klein_edit` | 参考图编辑(保人物) | img2+aud0+vid0 | `3-Flux2-klein全系列/flux-2-klein-单-多图编辑.json` |
| `flux2_klein_storyboard9` | 九宫格故事分镜 | img1+aud0+vid0 | `3-Flux2-klein全系列/九宫格Qwen3.5-Flux2-Kelin一键故事分镜.json` |
| `minimax_h3_i2v` | H3 图生视频 | img1+aud0+vid0 | `1-minimax H3/@MinimaxH3：图生视频 (4步加速版).json` |
| `minimax_h3_flf2v` | H3 首尾帧生视频 | img2+aud0+vid0 | `1-minimax H3/@MinimaxH3：首尾帧生视频（4步加速版）.json` |
| `minimax_h3_talk1` | H3 说话唱歌·单人 | img1+aud1+vid0 | `1-minimax H3/@MiniMax H3_ 说话唱歌 单人（4步加速版）.json` |
| `minimax_h3_talk2` | H3 说话唱歌·双人 | img2+aud1+vid0 | `1-minimax H3/@MiniMax H3_ 说话唱歌 双人（4步加速版）.json` |
| `minimax_h3_comic20` | 漫剧4宫格 | img5+aud0+vid0 | `1-minimax H3/@minimax-批量化漫剧20宫格-直出1分钟视频V3.json` |
| `minimax_h3_ref4` | H3 四图参考生视频 | img4+aud0+vid0 | `1-minimax H3/MiniMax H3  四图全能参考单采.json` |
| `minimax_h3_ref9` | H3全能参考(通用) | img9+aud0+vid0 | `1-minimax H3/MiniMax H3 全能参考(通用) 九图单采.json` |
| `grid4_stitch` | 四图拼四宫格 | img4+aud0+vid0 | `0-工具箱/四图拼四宫格.json` |
| `rmbg_cutout` | 人物提取 | img1+aud0+vid0 | `0-工具箱/人物提取.json`（自己写的，不是整合包自带。id 里的 `cutout` 是原名「抠图去背景」的残留，**别跟着改** —— 存档里存的是 id） |
| `rmbg_bgonly` | 抠出背景 | img1+aud0+vid0 | `0-工具箱/抠出背景.json`（同上；同一个节点开了 `invert_output`） |
| `rmbg_erase` | 人物擦除 | img1+aud0+vid0 | `0-工具箱/人物擦除.json`（同上；RMBG 认人 + Klein 补背景，出的是没洞的完整图） |
| `gimmvfi_interp` | 视频补帧插针(GIMM-VFI) | img0+aud0+vid1 | `SeedVR2图片视频高清/视频补帧插针(GIMM-VFI).json` |
| `seedvr2_image_up` | SeedVR2 图片高清放大 | img1+aud0+vid0 | `SeedVR2图片视频高清/SeedVR2图片高清放大.json` |
| `seedvr2_video_up` | SeedVR2 视频高清放大 | img0+aud0+vid1 | `SeedVR2图片视频高清/SeedVR2视频高清修复放大 v2.json` |

### 阶梯（ladder）：只差张数的工作流合成一个模式

同一张卡里，「只是图槽数量不同」的能力会被合并成**一个**模式，按用户实际传了几张图
自动路由。面板画的是图槽最多那条（超集），但参数默认值取「按当前张数真正会跑的那条」。

| 模式 | 传 1 张 → | 传 2 张 → |
|---|---|---|
| H3 图生视频 | `minimax_h3_i2v` | `minimax_h3_flf2v` |
| H3 说话唱歌 | `minimax_h3_talk1` | `minimax_h3_talk2` |

`minimax_h3_ref4` / `minimax_h3_ref9` **没有**参与合并：它们在 `SOLO` 里，各自独占一张卡，
所以不会互相吃掉。（`ref9` 覆盖 1~9 张，功能上已经盖住 `ref4`，是否退役 `ref4` 还没定。）

### 路由卡（route）：按素材**类型**路由的合并卡

登记在 `scan_workflows.py` 的 `ROUTE_CARDS` 里。一张卡、一格入口，放图片跑一条工作流、
放视频跑另一条：

| 卡片 | 放图片 → | 放视频 → |
|---|---|---|
| 画质增强 `card_enhance` | `seedvr2_image_up` | `seedvr2_video_up` |

**两路做的是同一件事** —— 都是 SeedVR2 重画式放大，只是一条吃图、一条吃视频。
「画质增强」问的是"清不清楚"，所以视频那一路是**视频放大**，不是补帧（补帧改的是
"顺不顺滑"，那是另一件事，有自己那张卡 `gimmvfi_interp`）。

**和 ladder 不是一回事。** ladder 是同一件事的不同张数，参数完全一样，所以能画一张"并集"
面板、提交时才落到具体那条。路由卡这边两条能力的参数对不上（视频那条多了帧率、
只处理前几帧、最长边上限），画不出并集面板 —— 所以是**放素材那一刻**就把
`c.cap` 切成对应的能力，切完面板、参数、产出类型全都走原来那套，前端不用额外知道什么。
切换时旧素材和连到这张卡的线一起清掉（槽位名从 `images[0]` 变成 `video[0]`，留着就是孤儿）。

一条能力可以同时挂两张卡（`gimmvfi_interp` 在 `SOLO` 里自己占一张卡），所以
`defOf()` 先认卡片自己记的 `c.type`，只有对不上时才去全局找 —— 老项目里存着
`type=card_enhance, cap=gimmvfi_interp` 的卡（示例项目的 `mv92pabe` 就是）靠这条
自动落回补帧那张卡，不会变成死卡。

### 风格卡（style）：唯一一张不对应工作流的卡

登记在 `scan_workflows.py` 的 `STYLE_CARD` 里，扫描器把它原样追加进 `_cards.json` 的末尾
（`kind: "style"`）。它**没有 manifest、没有能力**：`CAPS["style_text"]` 是空的，
所以前端凡是"先取 cap 再干活"的地方都先问一句 `isStyle(c)`（画卡、面板、运行、连线、菜单）。

它自己不跑，只存一段风格描述，连到哪张卡就在**提交那一刻**并进那张卡的提示词。
于是"整条链子统一一个风格"变成改一处：风格卡改一次，挂在它下游的卡全跟着变。

| 这件事 | 怎么做的 |
|---|---|
| 连线 | 还是 `PROJ.edges`，但 `slot` 写 `@style`。`@` 开头 = 传文字不传产物（`isTextEdge()`），所以搬文件那一套（`importOutput`、跑完自动喂下游）全绕开它。画成**紫色虚线** —— 画成同一根实线会让人以为风格卡也出图 |
| 挂给谁 | 只有**写提示词**的能力收（`promptSpecs()` 非空）。补帧、图片/视频放大、四图拼图这 5 条根本没有提示词框，挂上去等于没挂，直接拒并说明原因 |
| 合成 | 风格文字里写了 `{提示词}` 就把卡片自己的提示词填到那个位置；没写就**风格在前、空一行再接**。H3 要总基调在前、镜头约束在后，图生图那条的"提示词"其实是给描述模型的指令（风格得在后面）—— 一个占位符全都能表达，不用为每条能力写死规则 |
| 一张卡挂多张 | 按连线顺序依次套（后一张包住前一张的结果） |
| 往下传 | **只在第一棒挂一次，下游整条链都跟着套**（`styleCards()` → `upStyles()`）。顺的是**产物连线**，所以中间夹着不写提示词的卡（放大、补帧）也照样往下传 —— 那张卡自己用不上风格，但它不该把链子截断。同一张风格卡顺两条路汇到同一张卡只算一次 |
| 下游想换风格 | 那张卡自己挂一张，**就以自己的为准，上游那份不再往下传**（也不再传给它的下游）。两段风格叠一起会互相打架（"哥特暗黑"撞"清透日系"），而"这一棒换个风格"是真需求；两份都要就把上游那张也拖到这张卡上 |
| 还没写字的风格卡 | 不算（`ownStyles()` 过滤空文字）。先连线后写字是常事，那一阵子它不该出现在读数里，**更不该把上游的风格挡掉** |
| 漫剧那种一图一提示词的 | 5 条提示词**每条都套**（每个分镜都要同一个风格）。没配上图的那几条由服务端照 `pairWith` 清空，不会冒出一条只有风格的空分镜 |

**合并结果不写回 `c.params`。** 写回去风格就固化在那张卡上了：改风格卡时下游不跟着变，
再跑一次还会把风格叠第二遍。所以卡上那个框里永远是用户自己写的字，合并是提交时现算的
（`payloadOf()`）。代价是"面板上看到的 ≠ 真提交的"，所以提示词框下面加了一条只读的
**「实际提交」**读数（`.stypv`，跟分辨率/帧数那两条换算读数同一个路子）。

**继承是隐形的**（没有那根线），所以处处都得说出来。首先画在**线**上：风格借的就是那根
产物连线，所以捎着风格的产物线上再叠一层紫色虚线（`carriesStyle()` → `path.carry`），
一眼能看出风格传到哪一棒断掉（下游自己挂了风格，那根线之后就不再带紫）。另外：下游卡的「实际提交」标
「已套用 N 张风格卡 · 从上游继承」（悬停列出是哪张、怎么断）；风格卡卡脚标
「挂了 1 张卡 · 连带下游 3 张」；风格卡面板下半截把继承来的那几张列成**虚框**
（那一行的「继承」不是按钮 —— 要断得去断那根产物连线）；接产物线那一刻的 toast
补一句「上游的风格也跟着传下来了」。那个连带数是顺着连线算的，所以产物连线一改就要
`paintStyles()` 重刷（接线、清素材格、路由卡换路），不然数字停在改之前。

**跑过的那一轮要留证据。** 风格卡后来改了、解除了，那一轮就复现不出来了 ——
所以 `run()` 把真提交的那段整个记进 `history[].prompt`（只在它跟原词框里的字不一样时记 ——
套了风格卡、或者用的是「优化后」那份），右侧历史产物栏里折叠着一行「当时提交的提示词」。

### 提示词优化（提示词框右上角那个「✨优化」）

**为什么要有它。** 每个底模认的提示词长得完全不是一回事：Z-Image 文生图要一段自然语言，
MiniMax H3 要分段式（`integrated_multimodal_description` / `overall_soundscape` /
`non_diegetic_music`）外加镜头时间码和 `<Picture N>` 标签，参考生视频那几条还要
六段式（`subject_definitions` / `summary` / `retention_analysis` / …）。让用户自己背这些格式
不现实 —— 写成大白话直接跑，模型就只能猜。所以点一下按钮，交给大模型按规矩改一遍：
**先走 API（`chouka/llm.json` 里配的那家），不通再落本地** `Qwen3.5-27B-IQ4_XS.gguf`
（作者原图用的是 3bit 的 `UD-IQ3_XXS`，压得太狠，换成了同代的 4bit `IQ4_XS`；
unsloth 那个仓库里 3.5 **没有** `UD-IQ4_XS`，别去找）。两条路的分工见下面「跑法」。

**用的哪个大模型，两处必须一致。** 「优化」和「图生图」的反推共用同一个本地 27B。
llama-cpp 那个节点是**按 loader 配置缓存**模型的，两边差一个字段就会各自重新加载十几个 G，
所以：`server/rewrite.py` 的 `LOADER` 和 `server/scan_workflows.py` 的 `LLM_SWAP`
**要一起改**。`LLM_SWAP` 负责把工作流里写死的文件名换掉（不去手改 `graphs/`，那是产物；
也不动整合包自带的原始工作流）。它只在 ComfyUI 的下拉里确实有目标文件时才换 ——
模型没下好就保持原样、只在警告里说一声，硬换成不存在的文件名会让提交直接报错。

**换不成 Qwen3.8-27B，别再试了。** 包里的 llama.cpp 加载不了它，实测报
`missing tensor 'blk.64.ssm_conv1d.weight'`。原因：`llama-cpp-python 0.3.35` 的 `qwen35`
架构把「哪几层是注意力层、哪几层是 SSM 层」**写死**成每 4 层一个注意力层，`llama.dll` 里
也没有任何按层描述类型的元数据键（只有 `qwen35` / `qwen35moe` 两个实现）。Qwen3.5-27B 是
64 层（16 注意力 + 48 SSM）刚好对上；Qwen3.8-27B 是 65 层（17 + 48），第 64 层在文件里是
注意力层、loader 按 SSM 去找，于是失败。这跟量化档位、跟谁转的 gguf 都无关，任何
Qwen3.8-27B 的 gguf 都一样（3.8 还多出 MTP 的 `blk.N.nextn.*` 张量）。要用它只能升级
`llama-cpp-python`，那是整合包核心，不动。

`LLM_SWAP` 里 `Qwen3.8… → Qwen3.5-27B-IQ4_XS` 那两条是给分镜那条擦屁股的：它是子图工作流，
本地转不了，扫描器读的是磁盘上**已经导出**的 `api.json` —— 3.8 的名字曾经写进过那份文件，
只靠「旧 3.5 → 新 3.5」那条碰不到它，必须显式再列一遍。

**换模型的验证顺序**（Qwen3.8 那次踩的坑）：节点 `llama_cpp_instruct_adv` 写死
`verbose=False`，加载失败只会往上抛一句没信息的 `Failed to load model from file`，
llama.cpp 自己的真原因被吞掉。所以新模型下完，先在 ComfyUI **之外**单独
`llama_cpp.Llama(model_path=..., verbose=True)` 加载一次看真错误，通了再改配置重扫。
（`import llama_cpp` 在 python_embeded 里会报缺 ggml.dll，得先
`os.add_dll_directory()` 把 `site-packages/llama_cpp/lib` 和 `site-packages/torch/lib`
都加上。）

`chat_handler` 写 `Qwen3.5`，**不能**写 `Qwen3.5-Thinking`：节点拿
`"Thinking" in chat_handler` 当 `think_mode`，开了就会吐 `<think>` 段，
8192 的 `n_ctx` 还要被它吃掉一大半。视觉塔（`mmproj`）必须跟主模型同代。

**规矩从哪来。** H3 那两套是 MiniMax 官方的改写规范，不是我们编的：
`github.com/MiniMax-AI/MiniMax-H3` → `skills/h3-prompt-writing/references/`
里的 `base-en.txt`（T2VA/I2VA/FL2VA/L2VA）和 `ref-en.txt`（Ref2VA）。官方云端是拿一个叫
**H3-Context-IR** 的模型做这一步，出来的文本原样当 `prompt` 喂给 H3-Base ——
我们只是把这一步换成本地的 27B。规则文本已经浓缩进 `server/rewrite.py` 的 system prompt
（原文加起来两万多字，塞不进 `n_ctx 8192`）。

#### 一个提示词框，两份词

优化**不覆盖**用户写的东西。跑完这一格多一个页签，两份词都落盘（优化一次十几到几十秒，
刷新就没了说不过去）：

| 页签 | 框里是 | 生成时提交的是 |
|---|---|---|
| **原词** | `c.params[key]`，用户自己写的 | 原词 **+ 风格卡**（`withStyle`，提交那一刻现拼） |
| **✨优化后** | `c.opt[key]`，大模型改写的 | 这段**原样**，不再拼风格卡 |

选哪个页签写在 `c.optUse[key]`，`payloadOf()` 照它决定走哪条路。**关键在于风格只拼给原词那份**：
优化的时候风格文字是一起喂进去的（`rewrite.py` 的 `user_msg`，明确要求「融进输出里」），
所以优化后的词自带风格，提交时再拼一遍就是两份。页签右边一直写着
「已含风格，提交时不再另拼风格卡」—— 这条规则不写出来，用户只会以为风格丢了。

两个页签的框都能改（模型偶尔漏个标签、多写一句，当场补掉比重跑二十秒划算）。
「优化」的输入**永远是原词**，不是上一轮的优化结果 —— 拿优化结果再优化会一轮轮越写越长，
最后跟用户想要的没关系了。所以「重新优化」是拿原词换个种子重跑，覆盖旧的那份。
在「优化后」页签上，「清空」变成「扔掉这份」（删掉优化结果切回原词），
留一个空的优化结果在那儿只会让人对着空框发愣。

**风格卡后来动过怎么办。** 风格是**烙进**优化结果那段文字里的，之后改风格卡不会让它跟着变。
所以优化时把当时那段风格记进 `c.optStyle[key]`，页签那儿一对比就能说出话来：
风格卡是优化之后挂的 / 摘了 / 换了，三种都标黄提醒「点重新优化」。不然风格会静悄悄地错。

**四套规矩（`REGIMES`）。** 哪张卡用哪套写在 `scan_workflows.py` 的 `REWRITE` 表里，
扫描时落进 `manifest.rewrite`；前端只靠这一项决定画不画这个按钮。

| regime | 用它的能力 | 出来的东西 |
|---|---|---|
| `h3_base` | `minimax_h3_i2v`（I2VA）、`minimax_h3_flf2v`（FL2VA） | 对齐说明 + 三段式 |
| `h3_ref` | `minimax_h3_ref4`、`minimax_h3_ref9`、`minimax_h3_talk1`、`minimax_h3_talk2` | 六段式 |
| `zimage` | `zimage_t2i` | 中文，一段自然语言 |
| `klein` | `flux2_klein_edit` | 中文，一句编辑指令（点名图1/图2） |

**故意不给这个按钮的：** `zimage_i2i`、`flux2_klein_storyboard9`、`minimax_h3_comic20` ——
这三条的「提示词」本来就是给工作流里那个 27B 的指令，外面再套一层 27B 是脱裤子放屁。
四张工具卡根本没有提示词框。按钮也只画在**主提示词**那一格上（`promptSpecs(cap)[0]`）。

**Python 算的、不让模型算的那几件事**（`rewrite.py`）：

- **开头那句对齐说明**（`align_line()`）。官方 `base-en.txt` 明写它必须是**第一行**、后面空一行
  才是正文。里面的秒数是成片时长（卡上那个旋钮说了算）、`Shot N` 是最后一个镜头号 ——
  模型不知道成片多长，让它写必错。所以先把它写的那句掐掉，我们数完镜头再补上。
  图还没放的时候不补（对齐说明是在给参考图指位置），改成弹一条提醒说「放好图再优化一次」；
  喂给模型的硬性事实里也会换成「这条能力本该有图，用户还没放，你就当没有图、从零写开场」——
  不这么分开写，就会同时出现「`<Picture 1>` 就是 0 秒那帧」和「没有参考图，别提 `<Picture N>`」
  两句自相矛盾的事实。
- **标签统一成 `<Picture N>`**（`LABEL`）。模型爱写 `Picture 1`、`[Picture 1]`、`<picture1>`，
  而 `comfy/text_encoders/minimax.py` 只认 `<Picture N>` 这一种，别的当普通文字读 ——
  对不上就等于没点名，多张参考图会串。
- **`summary` 的方括号**（`SUMMARY`）。任务类型是个封闭集合，模型偶尔漏方括号，名字对着就直接补。
- **提醒**（`warn`）。点了 `<Picture 3>` 但卡上只放了 2 张图 → 弹一条 toast 说出来。

**`withStyle()` 认得那句对齐说明**（`H3_ALIGN`）：万一用户在原词框里自己手写了 H3 格式，
风格插到对齐说明**后面**去 —— 怼在最前面会把它挤到第三行，参考图就对不上位了。

**跑法：先 API，不通再落本地 27B。** 两条路只在「文本从哪来」这一步分岔 —— 喂进去的
system（`REGIMES`）和 user（`user_msg()`）一字不差，拿回来都过同一个 `clean()`。
分岔多一处，同一张卡两条路就会出两种格式，用户没法判断是模型写得不好还是链路串了。

| | 走 API（`server/llm.py`） | 落本地（`rewrite_local()`） |
|---|---|---|
| 快慢 | 几秒 | 模型已缓存 10~26 秒；**第一次要把 14 GB 挤进显存，好几分钟** |
| 显存 | 一点不碰 | 跟出图抢同一份 |
| ComfyUI | **不用开着** | 必须在跑，还要排队 |
| 上下文 | 人家给多少是多少 | `n_ctx` 只有 8192，H3 官方 39 KB 规则只能压缩着喂 |
| 任务面板 | 不登记（几秒就回，没什么可看） | 登记进 `JOBS`，能看见「提示词优化」在跑、能取消 |

**配置在 `chouka/llm.json`**（模板 `llm.json.例子`，照着复制改名填 key）。只讲 OpenAI 兼容的
`/v1/chat/completions`，不接各家 SDK —— DeepSeek / 智谱 / 通义 compatible-mode / Moonshot /
硅基流动 / 自建 one-api 全是这个接口，差别只有 `base_url` 和模型名，换一家不用改代码。
按 mtime 重读，改完不用重启。`/llm.json` 单独进了 `.gitignore`（**不能**指望它落在
被忽略的 `data/` 里就安全 —— 那目录是运行时产物，没人会去那儿找配置）。

模板里那一堆 `//` 说明是给人看的，所以 `load()` 用 `_strip()` 把**整行**注释掐掉、
用 `utf-8-sig` 读（记事本的 BOM 和注释是同一个报错「第 1 行第 1 列不认识」、两个原因）。
**只认整行**：行内的 `//` 一动，`"base_url": "https://…"` 就被截断，而且报的错会指向别处。
这条是踩出来的 —— 让用户"复制模板、改名、再手删 29 行注释"本身就不合理。

**失败一律落本地，原因带回前端**（用户选的就是这个）。`llm.ready()` 先挡一道：没填 key、
`enabled: false` 就直接走本地，不白等一次超时。超时默认 **90 秒**，是量出来的：自建网关跑
思考模型，`h3_ref` 那档（system 6.8 KB、要写满两段）实测 **60 秒**，填 20 就是每次都白等
20 秒再去跑好几分钟的本地。往大定不亏（兜底那条要几分钟），但别填十几分钟 —— 那就成了
两头时间都花。已覆盖的失败：HTTP 401/402/404/429（每个都翻成
人话，`HINT`）、超时、连不上、返回不是 JSON、没有 `choices`、正文空的（多半被内容审核拦了）、
`llm.json` 自己写坏了。两个额外处理：`<think>` 段掐掉（推理模型的思考混进 `content` 时，
`REGIMES` 要求"只输出提示词本身"，不掐会被当提示词提交）；`finish_reason == "length"`
不算失败但要警告（残缺的输出用户一眼看得出，而落本地要等好几分钟）。

**`RESERVE = 2048`：思考模型的思考段也算在 `max_tokens` 里。** 这个坑不看 `usage` 发现不了 ——
网关把思考放进 `reasoning_content`，`content` 看着干干净净，`<think>` 也没混进来，但
`h3_ref` 给 2400 额度、思考先吃掉约 2000，正文只剩 1399 字截在半句话上。`REGIMES` 那几个数
是照本地 27B 的 `n_ctx` 定的，那边不含思考段，不能直接拿来当 API 的上限。所以**只给 API
这条路加额度，不改 `REGIMES`**：`max_tokens` 是上限不是目标，不思考的模型写完就停，多给的
一个 token 都不会用、也不多花钱。唯一副作用是老模型（输出上限 4096 那种）可能回 HTTP 400，
`HINT[400]` 已经把模型名和 `max_tokens` 两种可能都提了。实测 4800 额度下 `h3_ref`
60.3 秒写完、耗时跟截断时一样 —— **慢的是思考本身，不是那个上限**。

**走了哪条路必须说出来**（`rwToast()`）：一次几秒和一次几分钟差两个数量级，不说用户只会
觉得"这功能时快时慢"，也不知道 `llm.json` 到底生效了没有。API 失败那句原因也要露出来 ——
key 填错、余额不足、超时是完全不同的处理。为这个把 `toast()` 改成按字数决定停留时长
（最多 9 秒）、`#toast` 加了 `white-space: pre-line`：一段 API 报错在 2.6 秒里读不完，
一闪而过等于没提示。

本地那条（`rewrite_local()`）：临时图现搭不落盘（`build_graph()`：`llama_cpp_model_loader` +
`llama_cpp_parameters` + `llama_cpp_instruct_adv` + `ShowText`）。加载器配置跟 `zimage_i2i`
里那个 `#79` **逐字一样**，模型是按配置缓存的，差一个字就是重新加载一遍 6.8G+9.4G。
`force_offload=True`：优化完把模型踢出显存，不然紧接着跑 H3（32B 文本编码器 + DiT）会 OOM ——
代价是连着优化第二次要重新加载。超过 15 分钟自己取消。这条任务跑完**不留在任务面板里**
（`finally` 里 pop）：结果和报错都当场回给了前端，面板上再挂一排「提示词优化」只会把真在跑的
出图任务挤下去。**ComfyUI 没开着时**报错要把两条原因一起说（「API 没通（…），本地兜底也
用不了：ComfyUI 没在运行」）—— 只报后半句会让人去查 ComfyUI，而真正该改的是 `llm.json`。

**没被这条链路覆盖的 LLM 调用。** 另有 3 处 `llama_cpp_instruct_adv` 是**嵌在工作流里**的，
不走 `/api/rewrite`，所以还是本地 27B：`zimage_i2i #81`（反推图片内容）、
`flux2_klein_storyboard9 #183:182`（看参考图写 9 段分镜）、`minimax_h3_comic20 #289`
（看四宫格写 JSON 剧本）。它们跟「优化」不是一类活 —— 三个都带 `images` 输入、
`inference_mode` 是 `images`/`video`，是**看图说话**，纯文本的 API 模型顶不了
（要 vision，而且得把图 base64 塞进 messages）。而且它们的输出是图里中间的一环，
下游还接着跑，换成 API 得在提交前把那三个节点摘掉、把文本当字面量写进下游 —— 另一件工程。

**这三处是「不做」，不是「还没做」。** 用户定的界线：出图这条路整条留在本地，只有「优化」
走 API。所以 `llm` 在 `app.py` 里只出现在 `api_rewrite()` 和 `api_health()` 两处，
出图的 `api_generate()` 一次都没碰过 —— 改动这里之前先确认还守着这条界线。

**历史记录。** 真提交的那段跟原词框里的字不一样时（并了风格卡、或者用的是优化后那份），
`run()` 把真提交的整段记进 `history[].prompt`，右侧历史产物栏折叠着一行「当时提交的提示词」。
不然那一轮复现不出来 —— 风格卡后来改了、优化结果被覆盖了都不影响它。

**实测**（走本地、模型已缓存）：文生图 10 秒、参考图编辑 16 秒、说话唱歌 23 秒、四图参考 26 秒。
走 API 的话这几条都是几秒，而且不占显存、ComfyUI 不用开。

### 各能力的槽位标签

| 能力 | 槽位 |
|---|---|
| `zimage_t2i` | 无 |
| `zimage_i2i` | 图片 |
| `flux2_klein_edit` | 图片 / 图片2（图片2 可空，空着就只拿一张图当参考） |
| `flux2_klein_storyboard9` | 图片 |
| `minimax_h3_i2v` | 首帧 |
| `minimax_h3_flf2v` | 首帧 / 尾帧 |
| `minimax_h3_talk1` | 图片 + 音频 |
| `minimax_h3_talk2` | 图片 / 图片2 + 音频 |
| `minimax_h3_comic20` | 1 / 2 / 3 / 4 / 5 |
| `minimax_h3_ref4` | 场景 / 角色 / 图片 / 图片2 |
| `minimax_h3_ref9` | 参考图1 … 参考图9 |
| `grid4_stitch` | 1 / 2 / 3 / 4（四个都必填） |
| `gimmvfi_interp` | 视频（`VHS_LoadVideo`，见 `VIDEO_LOADERS`） |
| `seedvr2_image_up` | 图片 |
| `seedvr2_video_up` | 视频（`VHS_LoadVideo`，见 `VIDEO_LOADERS`） |

路由卡 `card_enhance` 那一格空着的时候标签改成「素材」（`ROUTE_CARDS[...]["entry"]`），
一放进去就露出真名（图片 / 视频）—— 那正好是"路由到哪一路了"的反馈。

`minimax_h3_ref4` 的标签（场景/角色/图片/图片2）是从原工作流的节点标题抄来的，
四个槽其实都是等价的参考图，标签容易误导 —— 待改。

### 玩法说明（`NOTES`，鼠标停在卡片顶栏上才显示）

以前钉在参数面板最上面，一打开面板先是一大段字，把素材槽和参数全挤到下面去；
这段话看一遍就够了，现在挪到**悬停卡片顶栏**的那个浮层里（和「输入 / 可调 / 产出」
同一块，`app.js: hoverBrief` → `briefEl`）。面板底部留了一句「玩法说明：鼠标停在
卡片标题上」指路 —— 有 `note` 的能力才出这句。
（「现在放的是图片，会跑 xxx」那两条留在面板里：那不是介绍，是实时反馈，要对着参数看。）

只写「不知道就会跑废一轮」的用法：

- **`zimage_i2i`（图生图）**：它是**反推式**的 —— 先用一个视觉模型（`llama_cpp_instruct_adv`，
  27B 的 `Qwen3.5` GGUF，跟「优化」用的是同一份）把原图讲成一段话，那段话才是喂给采样器的提示词
  （`KSampler.denoise = 1`，原图的像素**完全不参与**）。所以出图是"同一个内容"，
  不是"把原图改一改"。面板上那个「提示词」写的其实是 `#81.custom_prompt` ——
  给描述模型的**指令**（默认「详细描述图像内容」，它会顶掉节点的 `preset_prompt`），
  想改画面就在这句后面追加要求；填成「一只猫」这种跟原图无关的话反而让它跑偏。
  出图尺寸跟着原图走（`GetImageSize` → `ImageScaleByAspectRatio V2.scale_to_length`），
  卡上**没有**画面比例/分辨率可调。第一次跑要加载 14 GB 的 gguf，很慢。
  想保住原图的人物和构图，把**重绘幅度**调到 0.4~0.6（见下面 `CAP_KNOBS`）；
  要人物几乎不变、只改指定的地方，那是「参考图编辑」干的活，不是这条。
- **`flux2_klein_edit`（参考图编辑）**：跟上面那条**机制完全不同** —— 参考图 VAEEncode 成 latent
  挂进 conditioning（`ReferenceLatent`，正负两条各挂一次），人物长相和画风由原图直接约束；
  提示词进的是 `#19 CLIPTextEncode`，是**真的画面提示词**，只用来说"改什么"。
  `denoise=1` 在这条里是对的，身份不靠 latent 保。
  放 1 张 = 保住这张图的人物和画风，只改你说的那部分（换背景/动作/服装/角度）；
  放 2 张 = 图1 是要改的画面、图2 是参考素材（换装、合影、换脸、把图2的人物挪进图1的场景）。
  提示词里要**点名「图1 / 图2」**，并且把要保住的东西**明写出来**，例如
  「让图1的人物换上图2的衣服，相貌、发型、光线和画风保持不变」。
  出图尺寸跟着**图1**走（`EasySizeSimpleImage` 最长边缩到 1280 → `EmptyFlux2LatentImage`），
  卡上没有比例可调。底模是 Flux.2 Klein 9B，跟 Z-Image 文生图不是同一个味道。
- **`minimax_h3_comic20`**：每张图都要是 2×2 的四宫格拼图，上传后自动切成 4 个分镜连成一段。
  传满 5 张 = 20 宫格。
- **`grid4_stitch`**：四张图按「左上 → 右上 → 左下 → 右下」拼成四宫格，就是上面那张卡要的素材。
  分镜顺序 = 槽位顺序。两张卡的「画面比例 / 分辨率」要调成一样的，切回来的每格才不会再被裁一刀。
- **`minimax_h3_ref9`**：多张参考图要在提示词里点名才不串 —— 第 1 张是 `<Picture 1>`，
  第 2 张是 `<Picture 2>`，依次往下。**图要从第一格开始按顺序填，中间留空会让后面的图顺位提前**
  （节点是按 dict 顺序消费 `ref_images` 的，空槽会被断线拔掉）。
- **`gimmvfi_interp`**：给视频「加中间帧」凑到你要的帧率，**时长不变**，所以是更顺滑，
  不是变慢也不是变长。很吃时间和显存，先拿几秒的短片段试。
- **`seedvr2_image_up`**：重画式放大，不是拉大像素，是照着原图重新画一遍，
  所以脸和字可能跟原图有出入，别用在要一模一样的场合。
- **`seedvr2_video_up`**：一帧一帧重画清楚。帧数不变、时长不变，所以**更清楚但不更顺滑**
  （要更顺滑找补帧那张卡）。非常慢，先「只处理前几帧」填 60 试一版再跑整段。

### 工具卡的参数（都写进了 `KNOBS`，面板上带说明）

| 参数 | 属于 | 界面名 | 范围 | 工作流默认 | 说明 |
|---|---|---|---|---|---|
| `target_fps` | 补帧 | 目标帧率 | 24–120 步进 2 | 48 | 出片就是这个帧率、时长不变。**不是倍数** —— 倍数和输入重采样由服务端反算，见下面「目标帧率」 |
| `frame_load_cap` | 补帧 / 视频放大 | 只处理前几帧 | 0–300 步进 15 | 0 | 0 = 整段。跑不动或想先看效果填 60（约 2 秒） |
| `resolution` | 图片/视频放大 | 放大到(短边) | 512–2048 步进 64 | 图 2048 / 视频 1024 | 出片**短边**的像素数，长边按原图比例跟着放 |
| `max_resolution` | 图片/视频放大 | 最长边上限 | 0–4096 步进 64 | 图 4096 / 视频 1920 | 长边超了就整体缩回来，**会盖掉「放大到(短边)」**。0 = 不限 |
| `scale_by` | 图片/视频放大 | 重画前先缩小 | 0.25–1 步进 0.05 | 图 0.5 / 视频 1 | 先缩再重画大。缩一点画得反而更细；1 = 不缩 |
| `force_rate` | 视频放大 | 帧率 | 8–60 步进 1 | 16 | 成片每秒多少帧、时长不变。见下面「帧率是一个旋钮写两个节点」 |

### 只给某一个能力开的旋钮（`CAP_KNOBS`）

`KNOBS` 是按 widget 名**全局**匹配的，有些名字在不同图里意思完全不同，开一个就得害一片。
这种就写进 `CAP_KNOBS[能力id]`，只在那一个能力上生效（`derive_inputs(api, oi, wid)` 里
`{**KNOBS, **CAP_KNOBS.get(wid, {})}` 一合）。

| 参数 | 只属于 | 界面名 | 范围 | 工作流默认 | 说明 |
|---|---|---|---|---|---|
| `denoise` | `zimage_i2i` | 重绘幅度 | 0.2–1 步进 0.05 | 1 | 1 = 完全照描述重画（原图只剩尺寸）。0.4~0.6 = 保住原图的构图和人物轮廓，只换质感和细节。再往下越像原图、提示词越使不上劲 |
| `sensitivity` | `rmbg_cutout` | 抠图灵敏度 | 0.5–1 步进 0.05 | 1 | 往下调把「模型不太确定」的地方也算成主体：发丝、纱、玻璃保得住，代价是边上留一圈背景残影 |
| `mask_blur` | `rmbg_cutout` | 边缘羽化 | 0–16 步进 1 | 0 | 边缘化开几像素，贴到别的背景上不像剪纸。2~4 就够 |
| `mask_offset` | `rmbg_cutout` | 边界收放 | -16–16 步进 1 | 0 | 整圈往里收（负）/往外放（正）。带了一圈原背景颜色就 -2~-4 |
| `process_res` | 三张 RMBG 卡共用 | 处理精度 | 512–2048 步进 128 | 1024 | 在「高级」里。模型就按 1024 练的，调高只对发丝/树枝/栅栏这种碎细节有用（`RMBG_RES`，三张卡共用一份说明） |
| `sensitivity` | `rmbg_bgonly` | 挖除灵敏度 | 同上 | 1 | **方向是反的**：往下调 = 主体挖得更干净，代价是发丝缝隙里的背景也被挖走 |
| `mask_offset` | `rmbg_bgonly` | 边界收放 | 同上 | 0 | **方向是反的**：正数 = 洞往外多挖。洞边留了一圈人的颜色就填 2~4 |
| `mask_offset` | `rmbg_erase` | 擦除范围 | **0–24 步进 2** | 8 | 擦不干净第一个该动的就是它（地上的影子、边上残留的轮廓）。**砍掉了负数半边**：往里收只会让人的边缘留在画面里，是纯粹的坏值 |
| `sensitivity` | `rmbg_erase` | 识别灵敏度 | 0.5–1 步进 0.05 | 1 | 管的是「哪些地方算这个人」（少条腿、漏个胳膊才动它），不管擦得多干净 —— 说明里点名跟「擦除范围」的分工，这俩最容易被当成一个东西 |
| `mask_blur` | `rmbg_erase` | 边缘过渡 | 0–16 步进 1 | 4 | 默认就是 4，不是 0：这里的羽化同时管新旧像素的接缝**和**绿色标记的边，调大接缝更软但模型对「擦到哪为止」也更含糊 |

`denoise` **不能**进全局 `KNOBS`：文生图和 7 条 H3 视频卡的图里都有一个
`BasicScheduler.denoise = 1`，那个调它只会跑废 —— 面板上多出一个"调了必坏"的旋钮，
比没有更糟（跟「孤儿节点不能当参数暴露」是同一个道理）。
它进了 `PRIMARY_KNOBS`（不折进「高级」）：它是玩法旋钮，是这张卡最该被看见的那一个。

`max_resolution` 破例进了 `PRIMARY_KNOBS`（不折进「高级」）：它不是玩法旋钮，但它能
**悄悄否决**正面的「放大到(短边)」—— 视频那条默认 1920，把短边拖到 2048 长边算出来
超限就整体缩回去，用户看到的是"拖了没反应"。能否决正面旋钮的开关必须也在正面。

`frame_load_cap`（只处理前几帧）也在正面：补帧和视频放大都是几十秒到几分钟的活，
「先跑 2 秒看一眼」是这两张卡的常规动作，不是高级技巧。它下面还带一行读数
（`augFrames()`）：**这段共 243 帧 · 24 fps · 10.1 秒 → 只跑前 60 帧**。滑条上限是个死数
（300），素材可能只有 243 帧 —— 不报总帧数，用户没法判断填的这个数是"截一小段"
还是"跟整段一样"。

帧数只能探文件：`api_upload` 对视频素材顺手探一次（`probe_video()`，用包里 imageio 带的
ffmpeg，`时长 × 帧率` —— 跟 VHS 自己用的 `CAP_PROP_FRAME_COUNT` 同一个来源），
把 `fps/frames/duration` 一起存进素材记录。这条改动之前存下的素材没有这几个字段，
面板会现探一次（`GET /api/media?ref=`）再补回记录里。

抠图那四个滑条的范围都比节点自己声明的窄（节点给 `sensitivity` 0–1、`process_res`
最高 4096）。窄下来的那一段是「能拖但拖了必坏」：灵敏度 0.5 以下整张图糊成一片主体，
`process_res` 上 2048 只是慢一倍、抠不出更多东西。前三个在正面，`process_res` 折进「高级」。

`model`（RMBG-2.0）和 `background`（Alpha）是 COMBO，扫描器一律不暴露 COMBO，
所以它们钉死在工作流里 —— 这正是想要的：换模型是另一张卡的事，`background` 一改
就不出透明底了。权重在 `models/RMBG/RMBG-2.0/`，本机跑，不联网、不限次数。

### 抠图是两张卡：主体一张、背景一张

`rmbg_cutout`（人物提取）和 `rmbg_bgonly`（抠出背景）是同一张图，差一个
`invert_output`：节点在合成之前把蒙版整张反过来，于是 `background: Alpha` 出来的
就从「主体不透明」变成「背景不透明、主体是个洞」。同一张原图两张卡各跑一次，
拿到的是能重新叠回原图的两层。

**没做成一张卡出两层**，虽然技术上可以（RMBG 的 MASK 出口接 `JoinImageWithAlpha`
就能白拿背景层，不用第二次模型推理）—— 但那样卡片就是 2 张产物、进宫格模式，
刚做的拖动对比线（只在单张产物时画）就废了。而这条能力最需要的恰恰是贴着原图看边缘。
两张卡各自单产物、各自带拖动线、各自一套说明，多花的只是几秒钟的第二次推理。

**四个旋钮在背景那张卡上方向全是反的**，所以 `CAP_KNOBS["rmbg_bgonly"]` 另写了一套
说明文字（照抄主体那张的话每一句都是反的，比不写更害人）。只有 `process_res` 跟蒙版
反不反无关，抽成 `RMBG_RES` 两边共用一份。

**主体位置那个洞这张卡不会填。** 想填洞的用第三张卡（`rmbg_erase` 人物擦除，见下节），
或者把背景层接给「图生图」/「参考图编辑」让它补。包里 `comfyui-rmbg` 其实带了
`AILab_LamaRemover`（big-lama 擦除补全，正是干这个的），没用上是因为它的权重
`models/RMBG/Lama/big-lama.pt` 不在包里，首次运行会去 HuggingFace 拉 200M+ ——
一张工具卡不该在用户按下生成之后才开始联网下载。要用就先把权重手动放进去。

### 第三张：人物擦除（`rmbg_erase`）—— 认人 → 涂绿 → 重画 → 贴回

抠图那两张出的都是**带洞**的图。这张出的是**没洞的完整图**：人不见了，那块地方按周围
环境补上了。全自动，用户不用手涂遮罩 —— 这是整条链子成立的关键，见下面 `DrawMaskOnImage`。

| 段 | 节点 | 干的事 |
|---|---|---|
| 认 | `RMBG` #10 → 只取 **MASK 出口（slot 1）** | 认出人在哪。`invert_output` / `refine_foreground` 都关：这里只要蒙版，不要它合成的图 |
| 对齐 | `EasySizeSimpleImage` #15 | 图和蒙版一起缩到最长边 1024，顺手把**宽/高**吐出来（slot 2/3）喂 `EmptyFlux2LatentImage` |
| 涂 | `DrawMaskOnImage` #20，`color: "0, 255, 0"` | 把蒙版区域涂成纯绿。**这一步是全自动的支点** |
| 重画 | `VAEEncode` → `ReferenceLatent` ×2 → `KSampler` #70（Klein 9B，6 步，cfg 1） | 照着"把绿色标记那块补掉"的指令重画整张 |
| 校色 | `ColorMatch` #85（`mkl`，强度 0.4） | 把重画结果的整体色调拉回原图 |
| 贴回 | `ImageCompositeMasked` #90 | **只把蒙版那一块**换成新画的，其余像素按位等于原图 |

**`DrawMaskOnImage` 吃真的 `MASK` 输入**，不是只能吃手涂的。原图 `flux-2-klein-局部重绘流`
是让用户在 LoadImage 上刷一笔当遮罩；把那根线换成 RMBG 的 MASK 出口，"手涂"就变成了
"自动认人"，而且**一个额外权重都不用下**（Klein 9B / flux2-vae / qwen3-8b 包里全有）。
这是这张卡能做出来的唯一原因 —— 专门干擦除的那两条工作流
（`新版Fill-移除、扩图、重绘三合一`、`8-Ltx2.3 视频去字幕+删除水印`）都缺 10 GB 级的权重，
`AILab_LamaRemover` 缺 200 MB 且要现下。

比原图多做的两件事：

- **`ImageCompositeMasked` 接上了**（原图里它是 bypass 的）。不接的话整张图都是 Klein
  重画的 —— 人擦掉了，但没擦的地方脸也变了、字也糊了。接上之后"没动过的地方真的没动过"
  这句话才成立，也才配得上那条拖动对比线。
- **不另加 grow/blur 节点**：RMBG 自己就有 `mask_offset` / `mask_blur`，一个节点出的蒙版
  同时给涂绿和贴回两处用，用户拖一个滑条两边一起变。

丢掉的：`RAMCleanup` / `easy cleanGpuUsed`（抽卡是连着跑的，不该在图里清显存）。
`device` 写 `"cpu"` 而不是原图里那个可疑的 `0`。

**这张卡的指令是写死的（`FIXED_TEXT`）。** 见下节。

### 卡片上的「原图 ↔ 结果」对比线（`COMPARE`）

`scan_workflows.py` 里有个 `COMPARE = {"rmbg_cutout", "rmbg_bgonly", "rmbg_erase"}`，进 manifest
的 `compare: true`；前端 `paint()` 认这个字段，把画面区换成两层图 + 一条能左右拖的分割线
（`cmpSrc()` / `applyCmp()` / `bindCompare()`）。**默认停在最左边 = 纯结果，
往右拖原图一点点长回来。**

只给这三张开：它们的成败全在边缘那一圈（抠干净没有 / 擦干净没有），不跟原图叠着看根本
判断不了，而透明底的图单看又只是一块空白（所以 `.cmp` 底下铺了灰格子 —— 卡片画面区
本来就近黑，不铺格子透明区跟"黑背景"看着一模一样）。图片放大 / 图生图 / 参考图编辑也都是
一进一出、画得出来，要开就往那个集合里加一个 id —— 但那几条看结果本身就够了。

拖动线的文案是**卡片中立**的（"跟原图对比" / "回到原图"），不能写"还原背景"：在
`rmbg_bgonly` 上往右拖回来的是**主体**，在 `rmbg_erase` 上回来的是**人**，三张卡共用同一段
DOM 和同一句提示。

拖到哪存在 `c._cmp`（下划线开头 → `plain()` 剥掉，不进存档）：这是「我刚看到哪」，
不是这张卡的设置。位置存百分比不存像素，卡片拉宽拉窄那条线不会跑偏。
`bindCompare` 必须用 `addEventListener` —— `.body` 的 `onmousedown` 已经被
`bindCardVideo` 占了，直接赋值会把视频的拖动定位顶掉。

### 「目标帧率」：一个框写三个节点（`to_target_fps` / `derive_target_fps`）

用户想的是"我要 60 帧的片子"，不是"补 2.5 倍"。而补帧节点只吃**整数**倍数，
成片帧率还是图自己算的（`VHS_VideoInfoSource` 的源 fps × 倍数）—— 源 24 fps 直接补
只能出 48 / 72 / 96，填 60 出 48，旋钮就是在骗人。

差的那一截交给上传节点的 `force_rate`：先把输入重采样到 `目标 ÷ 倍数`，再补这么多倍，
成片就正好是目标帧率。**时长始终不变**（`force_rate` 只改每秒取几帧，不改总时长）。
所以一个「目标帧率」要写三个地方：

| 写哪 | 写什么 |
|---|---|
| `#8.frame_rate`（成片） | 目标帧率本身。这个数**顶掉**原来那根算式的线，于是 `#33`/`#34` 没人依赖，ComfyUI 不执行它们 |
| `#32.Number`（那个 `Int`） | 倍数 = `目标 // 源fps`，夹在 `KNOBS[interpolation_factor]` 的 2~4 之间 |
| `#7.force_rate`（上传） | `目标 ÷ 倍数`。正好整数倍时填 **0**（= 跟着原视频，连重采样都省了） |

倍数**向下**取整不是四舍五入：这样 `force_rate ≥ 源 fps`，只会重复帧、不会丢帧。
`force_rate` 有节点上限 60，所以倍数还有个下限 `ceil(目标 / 60)`。
源 fps 要提交时才知道（`probe_fps()` 拿包里的 ffmpeg 探 `data/uploads/` 里那份），
探不到就用 2 倍 —— 帧率和时长照样准，只是重采样绕得多一点。

实测（源 24 fps、只处理前 60 帧）：目标 60 → `force_rate=30`、倍数 2 → 出片
**60 fps / 1.98 秒**，正好是那 2 秒源片。目标 48 → `force_rate=0`、倍数 2，最干净那条路。

换掉倍数旋钮的判据**不认工作流**（`to_target_fps()`）：有补帧倍数旋钮 + 有视频上传槽
（才有 `force_rate` 可用）+ 成片帧率是连线算出来的、且算式上游挂着那个倍数节点。
三条都对上才换 —— 视频放大那条的 `frame_rate` 是死数字，本来就有正常的「帧率」旋钮，
不能动。

倍数这个旋钮**从来不是直接改采样节点**：图里那个 `Int`（ComfyLiterals）同时喂
`GIMMVFI_interpolate.interpolation_factor` 和 `MathExpression` 的帧率乘法，两边必须一起变。
它靠 `literal_out()` + `fed_knob()` 认出来：**单输入、单个数字输出**的节点自己没有语义，
角色看它喂给谁（跟 Primitive 看 `RES_FIELDS` 是同一个思路）。

### 帧率是一个旋钮写两个节点（`RATE_IN` / `RATE_OUT`）

VHS 的「帧率」分在两个节点上：上传节点的 `force_rate` 把输入重采样成这个帧率，
合成节点的 `frame_rate` 给成片打上这个帧率。**两个必须始终相等**，只改一边成片就变成
快放或慢放。所以扫描器把 `frame_rate` 归到 `force_rate` 这一个 key 上（`knob_key()`），
面板只画一个框，第 2 个起标 `mirror`，提交时服务端照样写进每个节点
（视频放大那条有 3 个目标：`#968.force_rate` + 两个 VideoCombine 的 `frame_rate`）。

**只有两边都是死数字才开这个旋钮。** 补帧那条的 `frame_rate` 是算出来的
（`VHS_VideoInfoSource` 的源 fps × 补帧倍数），帧率由图自己管，插一手只会把时长算错 ——
所以那张卡上没有「帧率」，`force_rate=0`（跟着原视频）也就不会被暴露出来。

下限是 8 不是 0：`force_rate=0` 的语义是"跟着原视频"，可原视频多少帧要到运行时才知道，
而 `VHS_VideoCombine.frame_rate` 声明的 `min` 是 1、不收 0 —— 给一个能填 0 的框，
填了就是坏的。

### 一条工作流有好几个输出节点时取哪个（`is_preview()`）

`SeedVR2视频高清修复放大 v2` 有 4 个「输出」节点：成片
（`#1082 VHS_VideoCombine`，`save_output=true`）、放大前/放大后的单帧
`PreviewImage`、和一条左右对比的临时视频（`#1087`，`save_output=false`）。
按节点 id 取第一个会拿到 `#962 PreviewImage` —— 整条能力被判成**出图片**，
画布上拿回来的产物是原片的一帧，不是放大好的视频。

判据不认工作流：`PreviewImage` 天生是临时的；`VHS_VideoCombine` 这类自己带
`save_output` 开关，作者关掉它就是明说"这个只给我自己看"。真产物排前面、
临时预览排后面（同类保持 id 顺序），`scan()` 照旧取 `outputs[0]`。

补帧那条工作流里 `MathExpression|pysssss.expression = "a*b"` 声明成 multiline STRING，
本来会被认成「提示词」。`formula_node()` 挡掉了：**输出全是数字**的节点，它的多行文本框
是公式不是提示词。

### 写死的指令不是提示词（`FIXED_TEXT`）

```python
FIXED_TEXT = {("rmbg_erase", "40")}
```

命中的多行文本框在 `derive_inputs()` 里直接 `continue`，不进面板。

`人物擦除` 里 `#40 CLIPTextEncode` 那段"把绿色标记覆盖的人物整个去掉…"**不是用户的提示词，
是这条能力的实现细节** —— 它必须跟 `DrawMaskOnImage` 涂的那个绿色对得上。暴露出来有两种
死法，都很难看：

1. 用户当提示词写"一只猫"，绿色那块就长出一只猫，卡片名叫「人物擦除」却没在擦人。
2. 更糟的是**它会把卡片踢出「工具」组** —— `is_tool()` 的判据是"要素材、不写提示词"，
   面板上多一个 textarea 就等于宣布这是生成类，`kind` 掉回 `image`，卡片从 🧩 工具组
   落进「生图」的模式列表里（见开头 `is_tool()` 那节）。

**按 `(能力id, 节点id)` 挡，不按字段名。** `CLIPTextEncode.text` 在别的图里是真提示词，
按名字挡会把文生图一起哑掉；按节点 id 挡就只影响这一张图里的这一个节点。

要再写这种"作者钉死的指令"的卡，把 `(id, 节点)` 加进这个集合，顺手把节点标题写成
「…（写死的，别改）」—— 用户在 ComfyUI 里打开这张图时看到的是那个标题。

### 图里的「孤儿节点」不能当参数暴露（`prune_dead()`）

作者调完试完，图里常留下一些**跟谁都没连**的节点。ComfyUI 执行时是从输出节点反着往上
拉的，这种节点根本不会跑 —— 可它的小部件照样会被扫成旋钮。图生图那条里就躺着一个断开的
`EmptyLatentImage(1080×1920)`，照它画面板上会多出「宽 / 高」两个框，**拖了完全没反应**
（出图尺寸其实是 `GetImageSize` 量原图量出来的）。**一个能拖但没作用的旋钮比没有更糟。**

判据只看结构：不是输出节点（`object_info` 的 `output_node`）、输出又没人消费，就摘掉。
摘掉一个可能让上游也变孤儿，所以反复摘到不动为止。`ShowText|pysssss`、
`easy cleanGpuUsed` 这类自己声明了 `output_node=true`，不会被误摘。

**顺序要紧：`prune_dead()` 必须排在 `repair_*` 后面。** 漫剧那条的
`#203 easy lengthAnything` 本来就是个没人用的孤儿，它正等着 `repair_loop_count()` 接进
`#206.total`；先摘就把要修的东西摘没了，循环次数掉回写死的 1，只出第一段。

## MiniMax H3 参考素材上限（来自节点定义，不是猜的）

`ComfyUI/comfy_extras/nodes_minimax_h3.py` 里 `MiniMaxH3ReferenceToVideo` 的 autogrow 上限：

| 输入 | 上限 | 提示词里的点名标签 |
|---|---|---|
| `ref_images` | **9** | `<Picture i>` |
| `ref_videos` | 3 | `<Video k>` |
| `ref_video_audios` | 3 | — |
| `ref_audios` | 3 | `<Audio j>` |

标签都是**按类型各自 1 开始**编号的。`ref_image_size` 是 `match` / `max` 二选一。

## 加一个新能力要做什么

1. 把工作流放进 `ComfyUI/user/default/workflows/` 某个子目录。
2. 跑 `重新扫描工作流.bat`（= `python server/scan_workflows.py`）。它会自动：
   UI 格式转 API 格式 → 认出图/音频槽 → 认出可调参数 → 写出
   `graphs/<id>.api.json` + `manifests/<id>.json`。
3. **只有下面这些「从图里看不出来的知识」才需要手写**，都在 `server/scan_workflows.py` 顶部：

| 表 | 作用 |
|---|---|
| `ALIASES` | 工作流文件路径 → 稳定的能力 id（不然改个文件名 id 就变了） |
| `DISPLAY` | 能力 id → 界面上显示的名字 |
| `SOLO` | 点名「这个能力单独占一张卡」，不写就按产出类型归并（工具自动单独成卡，不用登记） |
| `ROUTE_CARDS` | 按素材类型路由的合并卡：一格入口，放图片跑一条、放视频跑另一条 |
| `STYLE_CARD` | 风格卡那张伪卡片定义。跟加能力无关，但「新建卡片」菜单是照 `_cards.json` 画的，卡片定义只该有一个来源，所以它也从扫描器出 |
| `VIDEO_LOADERS` | 哪些节点类是「上传视频」的入口（目前 `VHS_LoadVideo`），以及收文件名的输入名 |
| `REVIVE` | 允许把原工作流里 mute/bypass 掉的备用素材槽重新启用 |
| `CAP_KNOBS` | 只给某一个能力开的旋钮。widget 名在别的图里是另一个意思时用它（`denoise` 就是），别往 `KNOBS` 里塞 |
| `KNOBS` | 高级参数的推荐值和「为什么是这个值」的说明。**按 widget 名认，没有节点作用域** —— 加一条前先 grep 一遍 `graphs/`，同名不同义会在别的卡上冒出个错标签的旋钮（`batch_size` 在 SeedVR2 是"一批几帧"、在 `EmptyLatentImage` 是"出图张数"，所以没开出来） |
| `NOTES` | 玩法说明，鼠标停在卡片顶栏上才显示（不在参数面板里） |
| `COMPARE` | 这条能力的产物要不要在卡片上叠一条「原图 ↔ 结果」拖动对比线。只对单张图产物有效 |
| `FIXED_TEXT` | 按 `(能力id, 节点id)` 点名「这个多行文本框是写死的指令，不是提示词」，不进面板。少了它，人物擦除会因为多一个 textarea 被 `is_tool()` 踢出工具组 |

「✨优化」走远程 API 的配置**不在扫描器里** —— 它跟工作流无关，是 `chouka/llm.json`
（模板 `llm.json.例子`），由 `server/llm.py` 读，改完不用重启也不用重扫。
| `REWRITE` | 这条能力的提示词框上要不要「✨优化」按钮、用哪套规矩（`server/rewrite.py` 的 `REGIMES`）。提示词本来就是给某个 27B 的指令那几条别登记 |
| `LLM_SWAP` | 把工作流里写死的 gguf 换成别的（旧名 → 新名）。跟加能力无关，但**改它必须同时改 `server/rewrite.py` 的 `LOADER`**，两边配置不一致就会各自重新加载十几个 G。上面写了为什么换不成 Qwen3.8 |

**不要**为了某个工作流去改 `derive_inputs` 之类的通用逻辑 —— 那是所有能力共用的。

## 已知的小毛病

- `flux2_klein_storyboard9`、`minimax_h3_comic20`、`zimage_i2i` 的 `inputs` 里有**两条 `seed`**
  （指向两个不同的种子节点；图生图是采样器 `#70` 和反推用的描述模型 `#81`）。第 2 条起带
  `mirror`，面板只画一个框（跟「帧率」同一套机制），提交时两个节点拿到同一个值 —— 行为是对的。
  上限取所有节点里最小的那个。
- `flux2_klein_edit` 的出图尺寸**固定跟图1**（最长边 1280），卡上开不出比例/分辨率旋钮：
  管这件事的是 `EasySizeSimpleImage`，它的 widget 名是中文「缩放长度」，不在 `RES_FIELDS` 里。
  想改得动源工作流。另外它的默认提示词是原作者写的双图文案（「让图中两个人物互相搭肩…」），
  只放一张图时那句话对不上，得自己重写 —— 提示词的默认值是从工作流里抄的，扫描器不改。
- `flux2_klein_edit` 的源工作流里挂着 `flux-2-klein-NSFW.safetensors` LoRA，**包里没有这个文件**，
  但它是 bypass 状态（mode 4），扫描器不会把它带进 API 图 —— 转出来的 20 个节点里没有 LoraLoader，
  所以能跑。要是哪天有人在 ComfyUI 里把它启用了再导出，这条就会报缺模型。
- `flux2_klein_storyboard9` 是子图（subgraph）工作流，本地转不出来，用的是**已导出的**
  `graphs/flux2_klein_storyboard9.api.json`。改了源工作流后重扫描**不会**更新它，得重新导出。
- `minimax_h3_comic20` 的图里 `#206.total` 原本写死 1，扫描时已自动改接 `#203 easy lengthAnything`
  （按 `#296` 的条数循环），否则只会出第一段。
- **补帧靠环境补丁才跑得起来**（2026-08-24 已修，实测通过）。整合包原样是跑不动的，
  `GIMMVFI_interpolate` 报 `'CUDA_HOME' not set, unable to find cuda-toolkit installation.`
  —— 节点用 cupy 现场编译 CUDA 核，而包里没装 CUDA Toolkit，`nvrtc` 那个编译器 DLL 整个包
  都找不到。修法是补两个 pip 包 + 在 `1_1点击启动comfyui.bat` 里设两个环境变量，
  **详见 `ComfyUI核心补丁.md` 的「环境补丁：给 cupy 补上 nvrtc」**。
  整合包升级 / 一键修复之后那两行可能被冲掉，症状就是这句报错又回来了。
- 种子上限各节点不一样：`KSampler` 是 2^64，但 `SeedVR2VideoUpscaler.seed` 只到 2^32-1。
  随机种子必须按节点自己声明的 `max` 来掷（manifest 里的 `max` 字段），超了 ComfyUI 直接
  拒收整个任务（`Value … bigger than max of 4294967295`）。多个采样器共用一个种子输入框时
  取所有节点里**最小**的那个上限。

## 示例项目

`data/projects/9437290223cb.json`，名字「示例」，15 张卡，串成一条链：
角色 + 场景 → 九宫格分镜 → 6 个视频玩法，末尾接工具卡示范：

- `dmoI2I`「图生图｜反推重画」：接在角色图（`dmoA`）左边，走 `zimage_i2i`，有真实产物
  （`ComfyUI_00048_.png`，162 秒）。**提示词故意留空**用默认的「详细描述图像内容」，
  示范"只放图片就能跑"。产物跟角色图是同一个人设、不同细节 —— 那正好是反推式该有的样子。
- `dmoU1`「画质增强｜图片重画放大」：接在角色图后面，走 `seedvr2_image_up` 那一路，有真实产物。
- `a56i3pt6`「画质增强」视频那一路（`seedvr2_video_up`）：接在图生视频的产物后面，
  有真实产物（`SeedVR2 修复_00001-audio.mp4`，224 秒）。它的产物又接进 `p4mh90gn`（补帧），
  示范"先弄清楚、再弄顺滑"这条常见组合（307 秒）。
- `dmoU3`「补帧插针｜让视频更顺滑」：也接在图生视频后面，有真实产物
  （`目标帧率 = 60`，源 24 fps → 出片 **60 fps**）。那份产物是拿 `只处理前几帧 = 60`
  跑出来的（32 秒，只有前 1.98 秒），但卡上的参数**已经回到 0 = 整段** ——
  面板上不该摆一个"只跑一小段"的值，那是调试手法不是常规设置。重跑就是整段（两分多）。

它带 `"locked": true`：
- 前端启动时**默认打开它**（`web/app.js` 的 `boot()` 找 `p.locked`）
- 侧边栏不给它画删除按钮
- `DELETE /api/projects/{id}` 服务端也拒（403），不是只藏按钮
- **只读：一切改动都不落盘。** 用户在它上面拖卡、改参数、重跑都随便，
  但那些只活在这一次打开里，**刷新就回到这份固定的示例**。
  前端 `save()` 见 `PROJ.locked` 直接不发请求（否则每动一下弹一次「保存失败」），
  服务端 `api_project_save()` 也回 **403** —— 另一个标签页和写歪的脚本照样拦住。
  画布标题右边挂一枚橙色徽标「试玩画布 · 刷新还原」（`#ptitle .ro`）把这件事说在明面上：
  不然用户在示例上搭了半天，刷新全没了，只会以为是丢数据。

**要改示例本身就直接改 `data/projects/9437290223cb.json`**（PUT 这条路已经封了），
记得把 `rev` 加一 —— 不然开着旧快照的页面下一次写入会撞 409。

## 项目保存的并发保护

项目 JSON 有 `rev` 字段。前端每次保存整份 PUT，必须带上打开时拿到的 `rev`；对不上服务端回
**409**，前端丢掉本地快照重新加载。**`rev` 是必填的**，不带版本号的写入一律拒。

这条是踩过坑加的：以前没有版本号，一个开着旧快照的标签页随便点一下，就能把别处（另一个
标签页、或服务端脚本）刚写进去的产物整份抹掉。写脚本改项目时记得带 `rev`。
